name: Lighthouse CI

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: '0 16 * * 3'  # 每周三 UTC 16:00 (北京时间周四 00:00) 运行
  workflow_dispatch:      # 支持手动触发

jobs:
  lhci:
    name: Run Lighthouse CI
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install Lighthouse CI
        run: npm install -g @lhci/cli

      - name: Run Lighthouse CI
        env:
          LHCI_SERVER_BASE_URL: ${{ secrets.LHCI_SERVER_BASE_URL }}
          LHCI_TOKEN: ${{ secrets.LHCI_TOKEN }}
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 为定时任务创建唯一的构建标识
          if [ "${{ github.event_name }}" == "schedule" ]; then
            # 定时任务：使用时间戳作为唯一标识
            BUILD_TIME=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            BUILD_HASH="scheduled-$(date -u +%Y%m%d-%H%M%S)"
            
            # 设置自定义的构建上下文
            export LHCI_BUILD_CONTEXT__CURRENT_HASH="$BUILD_HASH"
            export LHCI_BUILD_CONTEXT__COMMIT_MESSAGE="定时任务 - $BUILD_TIME"
            export LHCI_BUILD_CONTEXT__COMMIT_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            export LHCI_BUILD_CONTEXT__AUTHOR="GitHub Actions (Scheduled)"
            
            echo "🕐 定时任务构建"
            echo "📦 Build Hash: $BUILD_HASH"
            echo "⏰ Build Time: $BUILD_TIME"
          else
            echo "📝 代码提交触发的构建"
          fi
          
          lhci autorun --config=lighthouserc.json

      - name: Send Feishu Notification
        if: always()
        run: |
          # 获取构建信息
          if [ "${{ github.event_name }}" == "schedule" ]; then
            # 定时任务
            BUILD_TIME=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            COMMIT_MSG="定时任务 - $BUILD_TIME"
            COMMIT_AUTHOR="GitHub Actions (Scheduled)"
            COMMIT_HASH="scheduled-$(date -u +%Y%m%d-%H%M%S)"
            BRANCH_NAME="scheduled"
          else
            # 正常提交
            COMMIT_MSG=$(git log -1 --pretty=%B)
            COMMIT_AUTHOR=$(git log -1 --pretty=%an)
            COMMIT_HASH=$(git rev-parse --short HEAD)
            BRANCH_NAME=${GITHUB_REF#refs/heads/}
          fi
          
          # 拼接 Dashboard URL
          DASHBOARD_URL="${{ secrets.LHCI_SERVER_BASE_URL }}/app/projects/lhci-test/dashboard"
          
          # 判断测试状态
          if [ "${{ job.status }}" == "success" ]; then
            STATUS_COLOR="green"
            STATUS_TEXT="✅ 测试通过"
          else
            STATUS_COLOR="red"
            STATUS_TEXT="❌ 测试失败"
          fi
          
          # 构建飞书消息
          curl -X POST ${{ secrets.FEISHU_WEBHOOK }} \
            -H 'Content-Type: application/json' \
            -d '{
              "msg_type": "interactive",
              "card": {
                "header": {
                  "title": {
                    "tag": "plain_text",
                    "content": "🚀 eSIM Lighthouse版本对比报告"
                  },
                  "template": "'"$STATUS_COLOR"'"
                },
                "elements": [
                  {
                    "tag": "div",
                    "text": {
                      "tag": "lark_md",
                      "content": "**测试状态**: '"$STATUS_TEXT"'\n**分支**: '"$BRANCH_NAME"'\n**提交**: '"$COMMIT_HASH"'\n**作者**: '"$COMMIT_AUTHOR"'\n**提交信息**: '"$COMMIT_MSG"'"
                    }
                  },
                  {
                    "tag": "hr"
                  },
                  {
                    "tag": "action",
                    "actions": [
                      {
                        "tag": "button",
                        "text": {
                          "tag": "plain_text",
                          "content": "📊 查看详细报告"
                        },
                        "type": "primary",
                        "url": "'"$DASHBOARD_URL"'"
                      },
                      {
                        "tag": "button",
                        "text": {
                          "tag": "plain_text",
                          "content": "🔗 查看 Actions"
                        },
                        "type": "default",
                        "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                      }
                    ]
                  }
                ]
              }
            }'
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}